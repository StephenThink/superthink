title: Link
sections:
  main:
    fields:
      -
        handle: title
        field:
          type: text
          required: true
      -
        handle: redirect
        field:
          type: link
          required: true
  sidebar:
    fields:
      -
        handle: slug
        field:
          type: slug
          required: true
          localizable: true
          validate:
            - 'unique_entry_value:{collection},{id},{site}'
